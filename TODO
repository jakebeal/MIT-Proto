- scons vs. opengl build failure  MOOT: NO SCONS
- make registry file, plugins go to the actual install directory DONE
- time model & distribution defaults need to be instantiated if
  not found in registry MOOT: ALWAYS SELF-REGISTER
- a physics layer is a layer that implements MOV_FN DONE
- add a default registry to the core project & its install DONE

- separate the plugins from the proto core project
- command-line option for plugin path for development
- command-line option for where to get the registry
